services:
  - type: web
    name: clv-intel-platform
    runtime: python
    rootDir: .
    buildCommand: pip install -r web/backend/requirements.txt
    startCommand: uvicorn web.backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
